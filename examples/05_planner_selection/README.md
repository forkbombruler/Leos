# Planner Selection Example

Use this example from Python to compare deterministic `PlanProposal` candidates.

The runtime planner expects structured proposals, scores risk/cost/benefit, and
selects a satisficing candidate without relying on free-form model text.
